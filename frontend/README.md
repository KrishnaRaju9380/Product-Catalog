# Product Catalog Frontend

Minimal React + Vite frontend that fetches products from the backend.

Quick start

```bash
cd frontend
npm install
npm run dev
```

Edit API base in `src/api.js` if backend runs on a different host/port.
