PORT=4000
NODE_ENV=development

# By default the project uses SQLite for local development (no DB server required)
# To use MySQL, uncomment and set the following values, then restart the app:
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=product_catalog
# DB_USER=root
# DB_PASSWORD=your_password
